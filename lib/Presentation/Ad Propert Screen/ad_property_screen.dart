import 'package:flutter/material.dart';

class AdPropertyScreen extends StatelessWidget {
  const AdPropertyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
